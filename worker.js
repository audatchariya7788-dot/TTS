const DEFAULT_PLAYER={level:1,hp:100,maxHp:100,gold:0,exp:0,stats:{str:10,agi:10,vit:10,int:5},equipment:{weapon:'Wooden Sword',armor:'Cloth Armor'},inventory:['Wooden Sword','Cloth Armor']};
export default{async fetch(request,env){const url=new URL(request.url);const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Methods':'GET,POST,OPTIONS','Access-Control-Allow-Headers':'Content-Type'};if(request.method==='OPTIONS')return new Response(null,{headers:cors});if(url.pathname==='/api/health')return Response.json({ok:true,service:'mmo-wom',game:'TTS 3D RPG',version:'v2'},{headers:cors});if(url.pathname==='/api/save'&&request.method==='POST'){const data=await request.json();if(env.GAME_SAVE)await env.GAME_SAVE.put('player:default',JSON.stringify(data));return Response.json({ok:true,saved:true},{headers:cors})}if(url.pathname==='/api/load'&&request.method==='GET'){const data=env.GAME_SAVE?await env.GAME_SAVE.get('player:default','json'):null;return Response.json(data||DEFAULT_PLAYER,{headers:cors})}return new Response('TTS 3D RPG API',{headers:cors})}};
